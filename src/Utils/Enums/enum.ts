export enum Gender {
  Male = "Male",
  Female = "Female",
}

export enum Rolle {
  User = "User",
  Admin = "Admin",
}

export enum Providers {
  System = "System",
  Google = "Google",
  Gmail = "Gmail",
}
