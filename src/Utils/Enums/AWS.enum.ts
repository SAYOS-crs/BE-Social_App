export enum AssetType {
  Profile = "Profile",
  Cover = "Cover",
  Images = "Images",
  Docs = "Docs",
  attachments = "attachments",
}

export enum FolderType {
  User = "User",
  Post = "Post",
  Comments = "Comments",
}
